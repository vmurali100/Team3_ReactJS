import './App.css';
// import { useState } from 'react';
// import ChildComp from './ChildComp';
// import ChildComp1 from './ChildComp1';
// import ChildCompo2 from './ChildCompo2';
// import ChildComp3 from './ChildComp3';
// import ChildComp4 from './ChildComp4';

// function App() {
//   const [allUsers, setallUsers] = useState([])
//   const getUsersFromChild = ((users) => {
//     setallUsers(users)
//   })
// ``
//   const [allUser, setallUser] = useState([])
//   const getUserChild = ((user) => {
//     setallUser(user)
//   })
  
//   const [allcolors, setallcolors] = useState([])
//   const getUserColor = ((color) => {
//     setallcolors(color)
//   })
//   const [allfruits,setallfruits] = useState([])
//   const getFruits = ((fruits)=>{
//     setallfruits(fruits)
//   })

//   const [allMobiles, setallMobiles] = useState([])
//   const getMobiles=((mobiles)=>{
//     setallMobiles(mobiles)
//   })
  return (
    <div className="App">
      {/* <ul>
        {allUsers.map((user, i) => {
          return <li key={i}>{user}</li>
        })}
      </ul>

      <div>
        <ul>
          {allUser.map((user, i) => {
            return <li key={i}>{user}</li>
          })}
        </ul>
      </div>

      <div>
        <ul>
          {allcolors.map((cr,i)=>{
            return <li key={i}>{cr}</li>
          })}
        </ul>
      </div>

      <div>
        <ul>
          {allfruits.map((frt,i)=>{
            return <li key={i}>{frt}</li>
          })}
        </ul>
      </div>

      <div>
        <ul>
          {allMobiles.map((mob,i)=>{
            return <li key={i}>{mob}</li>
          })}
        </ul>
      </div>

      <ChildComp recieveData={getUsersFromChild} />
      <hr/>

      <ChildComp1 sendData={getUserChild} />
      <hr/>

      <ChildCompo2 sendColor={getUserColor}/>
      <hr/>

      <ChildComp3 sendfruits={getFruits}/>
      <hr/>
      
      <ChildComp4 sendmobiles={getMobiles}/> */}

      {/* <Sample1/> */}
    </div>
  );
}

export default App;
